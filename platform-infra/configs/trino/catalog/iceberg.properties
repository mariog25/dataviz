connector.name=iceberg
iceberg.catalog.type=rest

iceberg.rest-catalog.uri=http://polaris:8181/api/catalog
iceberg.rest-catalog.warehouse=local_lakehouse
#iceberg.rest-catalog.prefix=local_lakehouse

iceberg.rest-catalog.security=OAUTH2
iceberg.rest-catalog.oauth2.token=${ENV:POLARIS_TOKEN}
#iceberg.rest-catalog.oauth2.server-uri=http://polaris:8181/api/catalog/v1/oauth/tokens
#iceberg.rest-catalog.oauth2.credential=root:secret
#iceberg.rest-catalog.oauth2.token-refresh-enabled=true

iceberg.file-format=PARQUET

# MinIO (S3-compatible) for data access
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.path-style-access=true
s3.aws-access-key=${ENV:MINIO_ROOT_USER}
s3.aws-secret-key=${ENV:MINIO_ROOT_PASSWORD}
